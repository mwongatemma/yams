A postgres superuser needs to run the user, database and schema creation
script.  The the collectd user can run the table creation script.

Example:

psql -U postgres -d postgres -f create-user.sql
psql -U postgres -d postgres -f create-database.sql
psql -U collectd -d collectd -f create-schema.sql
psql -U collectd -d collectd -f create-tables.sql
